//An array is a collection of values that are stored under a single
// variable name.
// Arrays can hold any type of value, including numbers, strings and
// even other arrays.
// Each value in and array is accessed through its index which starts at zero for
// the first value in the array. 

let fruits = ['apples', 'oranges', 'bananas'];
console.log(fruits[0]); //output apples

//built in methods for arrays

fruits.push('grapes'); //adds to end of array
fruits.pop(); //removes the back of array
//there more methofs for arrays

//create array of mixed datatype
let mixed = [1, 'apple', true, {name: "john"}];
